<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <%- include('style'); %>
    <!-- <link rel="stylesheet" href="/index.css" /> -->
  </head>
  <body>
    <%- include('nav'); %>
    <main>
      <h1>Update <em><%= book.title %></em></h1>
      <form action="/update/<%=book.book_id%>" method="POST" id="add-form">
        <div>
          <label for="title">Title</label>
          <input type="text" name="title" value="<%= book.title%>" required />
          <div style="font-size: 0.75rem">Case sensetive</div>
        </div>
        <div>
          <label for="author">Author</label>
          <input
            type="text"
            name="author"
            value="<%= book.author_name%>"
            required
          />
          <div style="font-size: 0.75rem">Case sensetive</div>
        </div>
        <div>
          <label for="genre">Genre</label>
          <input
            type="text"
            name="genre"
            required
            value="<%= book.genre_name%>"
          />
          <div style="font-size: 0.75rem">Single genre</div>
        </div>
        <button type="submit">Add</button>
      </form>
    </main>
  </body>
</html>
